// ilk girisi duzelt ekrana apple ucun login google (instagram) belke instagrama story kimi gozel gorunum atmaq ucun
// google ile en problemli hissedilen vaxt ucun xestaxana add et

// seflik olmamasi adina bunu duzeltey
// YENILENDI (2026-08-19): web (Next.js) tam silindi, layihe artiq yalniz Flutter-dur.
// Kohne Next.js strukturu (src/, app/, components/ ve s.) artiq YOXDUR.
// Hazirki struktur:

lifegrapher_mobile/
├── lib/
│   └── main.dart                # BUTUN tetbiq kodu (ekranlar, melumatlar, rengler)
├── assets/
│   └── logo.png                 # tetbiq loqosu (giris ekrani + ikon)
├── test/
│   └── widget_test.dart         # tetbiq testi
├── android/                     # Android-e xas fayllar (el vurma)
├── ios/                         # iPhone-a xas fayllar (el vurma)
├── build/                       # Flutter-in oz yaratdigi fayllar (HEC VAXT deyisme)
└── pubspec.yaml                 # paketler + assets siyahisi (deyisende: flutter pub get)

// Esas komandalar:
//   flutter run        - tetbiqi simulatorda acir
//   flutter pub get    - paketleri yukleyir (pubspec.yaml deyisenden sonra)
//   flutter analyze    - kod sehvlerini gosterir
//   flutter test       - testleri isletir
//   flutter clean      - qeribe xeta olanda temizleyir
// Tam beledci: notes/flutter-baslangic.md

//
// Not: Menim qeydim. Bu hisseler toxunulmasin.
// - .gitignore
// - tracker.py
// - logs/ klasoru
// - github/ klasoru
// - data/ entries.json
// - scripts/ klasoru
// - notes/ klasoru (yalniz qeyd ucun)
// - README.md ve proje strukturunu deyismek, haminin changelere toxunmaq
//
// Qeyd: GitHub ve repo ile bagli deyisiklikler, commit, push ve silme emeliyyatlari
// yapmamaliyam. Bu gunde men yalniz not aliram ve fikirleri qeyd edirirem.
//
// Note to self: do not touch core repo config/files while taking notes.
// Only write in notes, journal, and work notes. Keep Git operations and code changes for later.
